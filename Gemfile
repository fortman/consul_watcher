source "https://rubygems.org"

repo_name = '/fortman/consul_watch_to_amqp'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in consul_watch_to_amqp.gemspec
gemspec
